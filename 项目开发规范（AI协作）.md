# 项目开发规范（AI协作）

当前项目规则见 `AGENTS.md`。本仓维护独立可安装的协作技能，不实现任务流程引擎。

修改前核对当前源文件、用户已有差异与授权范围。规则应补充具体项目知识、需求判断、证据或权限边界；不要用固定步骤替代模型的常规规划能力。

正文、元数据、参考资料、示例和安装说明必须含义一致。需要删除的能力应连同可执行入口、内部模块、模板和失效测试一起处理，不把未删除实现称为历史资料后继续分发。

保留安全边界和必要备份。脚本能力被整体移除时，其专用测试随之退出；仍在交付的功能不能通过缩小 CI 范围绕开失败。新测试应检查包边界、独立安装、引用、编码、授权和真实用户场景。

完整本地测试：`python -B -m unittest discover -s tests -v`。另检查 `git diff --check`。静态规则测试与模型行为评估是不同证据；不得把前者说成后者。

修改长期事实时更新已有合适说明；不复制目录清单或对话历史。临时产物应范围有限，不重复生成构建文件，不把备份或本地状态收入包。
