# 项目完整链路说明

## 用户使用

宿主按技能元数据发现适用能力，在用户调用或需求匹配时读取相应 `SKILL.md`。核心 DXM 明确本次范围、读取已有规则和证据，仅在重要未知阻塞下一步时澄清；在授权范围内维护必要规则并报告验证与未完成项。

核心包独立使用。可选访谈与领域技能不存在时，按核心内联约定处理，不自行安装。参考资料只在相关风险或任务出现时加载。

普通开发任务不必重新调用 DXM。项目已有规则继续适用，但没有隐藏任务初始化、回执检查或额外流程引擎。

## 维护与发布

维护者修改技能、元数据、参考资料或示例，更新适用测试和文档，然后运行整个测试目录与差异检查。CI 配置覆盖 Ubuntu、Windows、macOS 上的 Python 3.10、3.12、3.14；配置存在不等于远端 CI 已执行通过。

版本文件表示当前源码版本。Git 提交、远端推送、标签、发布和已安装副本必须分别验证，不能互相替代。具体授权与核验见 `开发者AI开发与PR提交流程.md`。

已安装副本和打开的会话可能仍使用旧内容。更新源码不会自动修改用户级技能或刷新会话；升级按 `docs/migration-v3.md` 处理。

## 标签发布链路

维护者获得发布授权后，更新版本和说明，提交并推送 main 及对应正式标签。九组 CI 测试全部通过后，发布任务核对标签、主分支和版本一致，构建核心及完整技能包，创建或续传未公开的 Release 草稿，下载回验通过后公开并设为 Latest，随后再次下载核验并写入真实发布结果。已公开的同名版本不自动覆盖。
