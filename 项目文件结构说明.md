# 项目文件结构说明

> 项目：`dxm`
> 根目录：当前仓库根目录（本机规范化路径记录在 `.dxm/project.json`）
> 初始化日期：`2026-07-09`

本文档列出当前项目维护视角下的文件结构与职责。新增、删除、重命名源码、配置、长期文档后，必须同步更新本文。

<!-- DXM-DOC-RULES:START -->

<!-- DXM-CONTRACT:2 -->

## DXM 文档维护规则

- 本块由 DXM 管理；`--refresh-blocks` 只刷新本块，保留下方文件职责和人工补充。
- 新增、删除、重命名文件或改变目录职责后，直接更新下方对应条目，不堆叠临时增量说明。
- 运行态、缓存、账号、密钥、环境变量和构建产物只说明用途与保护边界，不回显真实内容。
- 当前根目录快照是初始化种子；首次 `/dxm` 后需要根据真实子目录和模块职责继续补充。

<!-- DXM-DOC-RULES:END -->

## 维护原则

- 文件职责发生明显变化时，必须更新对应说明。
- 运行态数据文件可以记录用途，但不得写入真实 token、密码、API Key、账号明细或会话内容。
- 本文档不保留“增量补充”堆叠章节；结构变化时，直接更新对应文件条目与职责说明。

## 根目录长期文档

- `AGENTS.md`：目录级 AI 执行规则，定义 `/dxm` 触发与本目录强制遵守事项。
- `项目开发规范（AI协作）.md`：AI/开发者协作规范、架构边界、测试规范、文档同步和乱码检查要求。
- `项目完整链路说明.md`：项目从配置、输入、执行到输出的完整运行链路。
- `项目文件结构说明.md`：项目文件结构与职责边界。
- `开发者AI开发与PR提交流程.md`：分支、PR、GitHub CLI、合并授权等协作流程说明。

## 当前根目录快照

- `.git/`：Git 仓库数据；不展开维护。
- `.github/`：CI 配置；`workflows/ci.yml` 在 Ubuntu、Windows、macOS 的受支持 Python matrix 上运行 `tests/`、packaged self-test 与 core entry-point smoke。
- `skills/dxm/`：可独立安装的核心 skill；`VERSION` 保持 core-only doctor 版本与仓库发布版本一致，`SKILL.md` 定义四模式与完成门，`contract/policy.json` 定义受限 profile/limit，`scripts/dxm.py` 是产品命令面，`dxm_io.py` 提供可恢复写入与统一 lock/journal recovery inspection，`dxm_git.py`/`dxm_inventory.py` 处理本地状态隐私与不可信快照，`dxm_contract.py` 负责 baseline v1/run v1/readiness/receipt v2 契约，`validate_dxm.py` 提供只读 CLI，`assets/templates/` 提供五份自包含长期文档，`agents/openai.yaml` 提供当前 metadata schema。
- `skills/grilling/`、`skills/grill-with-docs/`、`skills/grill-me/`、`skills/domain-modeling/`：独立安装的 optional 增强；不属于 core DXM 运行硬依赖，其中 `grill-me` 是 legacy alias。
- `tests/test_scaffold_dxm.py`：安全写入、root/祖先路径防逃逸、baseline hydration、marker、Trellis 启动/退出语义和兼容行为。
- `tests/test_dxm_contract.py`：baseline/readiness/run/receipt v2/legacy audit/validator CLI 契约。
- `tests/test_dxm_v2.py`：P0 readiness exit、Git privacy、lock/journal recovery、安全库存、portable schema/path、profile 与路由回归。
- `tests/test_policy_contract.py`：四模式、0–3 问题预算、evidence matrix、selective docs 与 frontmatter。
- `tests/test_doc_sync.py`：metadata、README、core-only packaging 以及 skill/template/script 防漂移。
- `tests/test_workflow_cases.py` / `tests/fixtures/dxm_workflow_cases.json`：绑定真实策略资产的 synthetic 行为案例、Windows/UNC 路径和隐私扫描。
- `.dxm/project.json`：本机 dogfood 的规范化项目基线；它保留绝对 root，但由 `.gitignore` 排除，不发布。共享链路文档先词法折叠路径，再只保留 `$PROJECT_ROOT` / `$ABSOLUTE_PATH` 可移植 managed block 投影。Git 已跟踪 `.dxm/` 必须人工解除跟踪，DXM 不会自动改 index。
- `.dxm/runs/<run_id>/`：本机可写任务的 lightweight `run.json`、run-only `completion.json` 与适用时的 canonical `independent-review.md`；记录原始目标、scope、outcomes、baseline impact、risk 与证据绑定，由 `.gitignore` 排除，不发布。
- `.dxm/locks/` / `.dxm/transactions/`：每个根目录的写入互斥锁及可恢复 journal/backup；只用于本地控制和 recovery，不发布。
- `.trellis/`：本机中大型任务 PRD、上下文、check、canonical `independent-review.md`、workspace 与 Trellis 工具；任务归档和 receipt 属于本地验证证据，由 `.gitignore` 排除。
- `.codex/` / `.agents/skills/`：Trellis 为 Codex 生成的本地 agents、hooks 和共享 skill 入口；不是 core DXM 或 release artifact。
- `.gitattributes` / `.gitignore`：行尾与忽略规则；模板和中文文档要求 LF，本机 `.codex/`、`.dxm/`、`.trellis/`、`.agents/` 不进入发布。
- `AGENTS.md`：本仓自身的 DXM 目录级执行规则（dogfood，2026-07-09 起）。
- `CHANGELOG.md` / `VERSION`：发布记录与当前版本号；发布流程见 `开发者AI开发与PR提交流程.md`。
- `LICENSE` / `README.md`：许可证与面向用户的 core-only 安装、baseline/validator、readiness、receipt、可选 skills 和 Trellis 说明。
- `项目完整链路说明.md` / `项目开发规范（AI协作）.md`：DXM 长期文档；规范文档含本仓专属"三处契约同步规则"。

> 上方快照为自动生成的顶层种子，只能作为起点；维护者应继续补充关键子目录、入口文件和运行态目录的职责。

## 文件维护边界

首次 `/dxm` 生成后，应根据真实项目补充：

- 主入口文件负责什么，哪些逻辑不应继续放入主入口。
- 主要源码目录、测试目录、脚本目录、配置目录分别负责什么。
- 运行态数据、缓存、构建产物、依赖目录如何保护或忽略。
- 修改中文日志、注释或文档后如何做乱码检查。

<!-- DXM-TRELLIS:START -->

## DXM 大开发工作流目录

本项目启用 Trellis/Codex 大开发工作流时，下列目录属于项目级 AI 协作基础设施：

- `.trellis/`：Trellis 项目工作流状态、任务 PRD、spec、workspace journal 和脚本。
- `.dxm/runs/`：可写任务的 lightweight run、任务 outcome/impact 边界、inline completion receipt 和适用时的 canonical `independent-review.md`；默认由项目忽略，不作为发布产物。
- `.trellis/tasks/`：每个开发任务的 `task.json`、`prd.md`、实现上下文和检查上下文。
- `.trellis/spec/`：可复用项目规范；完成任务后应把稳定经验沉淀回这里。
- `.codex/`：项目级 Codex agents、hooks 和配置。
- `.agents/skills/`：跨 agent 共享的 Trellis skill 入口。

维护要求：修改这些目录的事实结构或工作流含义时，同步更新 `AGENTS.md`、`项目开发规范（AI协作）.md` 和本文档。

<!-- DXM-TRELLIS:END -->
