# 项目文件结构说明

`skills/dxm/` 是核心可安装包，包含技能入口、版本、许可证、显示元数据、一份可选规则示例和按需工程参考；不包含运行时脚本或任务契约。

`skills/grilling/`、`skills/grill-me/`、`skills/grill-with-docs/`、`skills/domain-modeling/` 是可选独立能力；最后一个目录附带术语与决策格式参考。

`tests/` 检查当前包、引用、文本和关键规则文字。它是静态回归检查，不是实际模型行为评估；没有已执行的模型评估结果。

`.github/workflows/ci.yml` 执行完整当前测试集。`docs/migration-v3.md` 说明破坏性升级、旧安装清理和项目历史数据保护。`docs/reviews/` 保存有证据的审查记录。

根目录的项目说明服务于本仓维护；`VERSION`、`CHANGELOG.md` 和 Git/PR 说明记录版本、历史与发布边界。备份、用户安装、副作用测试产物和历史运行数据不属于分发包。

`tools/package_release.py` 负责维护者发布时的精确文件白名单、可重现 ZIP 和校验清单；不进入技能包。`docs/releases/` 保存中文版本说明。CI 的发布任务只在正式版本标签且完整测试通过时执行。
