# 项目完整链路说明

> 项目：`{{project_name}}`  
> 根目录：当前仓库根目录（`init` 后的本机规范化路径记录在 `.dxm/project.json`）
> 初始化日期：`{{generated_date}}`

本文档说明当前项目从输入、配置、启动、核心处理到产出的完整链路。开发前应先读 `项目文件结构说明.md`，再读本文，最后对照 `项目开发规范（AI协作）.md` 做变更。

<!-- DXM-DOC-RULES:START -->

<!-- DXM-CONTRACT:2 -->

## DXM 文档维护规则

- 本块由 DXM 管理；`--refresh-blocks` 只刷新本块，保留下方项目事实和人工补充。
- 项目目标、运行模式、配置链路、故障归因等长期事实必须写入下方对应章节，不能只留在聊天或临时文件里。
- 敏感配置和运行态输出只写用途，不回显真实 token、密码、API Key、账号明细、验证码、会话或密钥内容。
- 修改中文内容后必须检查可见乱码、错码和异常替换字符。

<!-- DXM-DOC-RULES:END -->

## 1. 项目目标

首次 `/dxm` 生成后，应在这里写清楚：

- 项目解决什么问题。
- 主要用户或调用方是谁。
- 成功产物是什么。
- 哪些文件、目录或外部服务属于运行态输入/输出。

## 2. 运行模式

列出项目真实存在的运行模式，例如：

- 正常启动 / 批处理 / 单次任务。
- 开发模式 / 测试模式 / 调试模式。
- 补偿任务 / 迁移任务 / 后台任务。

每种模式都应说明入口命令、关键配置、输入、输出和停止条件。

## 3. 配置加载链路

记录配置从哪里读取、如何归一化、默认值在哪里、哪些字段敏感、哪些字段会影响流程。

不得在本文档中回显真实 token、密码、API Key、账号明细或会话内容。

## 4. 核心流程

按真实执行顺序描述：

1. 启动与预检。
2. 读取配置与输入。
3. 初始化服务、客户端、浏览器、数据库、队列或外部 provider。
4. 主业务阶段。
5. 状态更新、输出写入和清理。
6. 异常处理、重试、回退和停止条件。

如果流程有多个阶段，给每个阶段明确边界：它负责什么、不能处理什么、成功信号是什么、失败归因在哪里查。

## 5. 模块与边界

记录核心模块之间的职责边界。新增页面状态、provider、API、CLI、后台任务或数据处理分支前，必须先确认它属于哪个真实阶段或模块。

## 6. 输出文件与状态流

记录所有运行态输出文件、数据库表、缓存目录、日志文件、产物目录的用途和状态含义。

DXM 本地治理状态至少包含 `.dxm/project.json` 和可写任务的 `.dxm/runs/<run_id>/run.json`；run-only completion 位于同目录。run 用 `baseline_impact` 区分 `affected` / `not_affected`，不得把未触及的长期验收项伪装成本次 passed。Git 项目通过 `.gitignore` 的 DXM 托管块忽略这些本地状态；若它们已被 Git 跟踪，audit 必须 BROKEN 并只给出人工修复建议。

写入链路为：预检所有受管路径/marker → 取得项目锁 → journal 记录旧/新 digest → 同目录临时文件 `fsync` 后 `os.replace` → 提交或 rollback。journal 绑定安全 operation ID、文件名、entry schema 和 backup layout；recovery 拒绝 state link/reparse、错配或未验证 journal 后才会触碰文件。任何中断遗留都必须先运行 `scaffold_dxm.py --recover --root <root>`；不得以再次 scaffold 掩盖未完成事务。`audit`、`doctor` 与 completion receipt 对 active/stale/malformed lock、pending 或残留 committed journal 一律不报 READY。`--refresh-blocks` 发现已有有效 `.dxm/project.json` 时只重新水合其链路 baseline block，不重写该 JSON。显式 `init` 的退出码是最终 readiness，而 `--output json` 分别携带 operation_status、readiness、实际 exit_code、readiness_exit_code 和 issues；已请求 JSON 的参数边界错误也使用这一结构和 `DXM_E_INVALID_ARGUMENTS`。

敏感输出只能说明用途，不能复制真实内容。

## 7. 常见故障归因

维护常见错误到排查入口的映射，例如：

- 配置缺失或格式错误。
- 网络、代理、外部服务失败。
- 登录、授权、验证码、回调、浏览器状态异常。
- 数据库、文件写入、权限或路径异常。
- 测试、构建、依赖安装失败。

每条故障应说明优先查哪个日志、哪个文件、哪个命令或哪个运行态证据。

## 8. 测试与验证

记录当前项目真实可用的测试、静态检查、启动验证和人工验收方式。

可写任务先用 `validate_dxm.py run --root "<project-root>" --file .dxm/runs/<run_id>/run.json` 校验目标与范围。completion receipt 使用 `schema_version: 2`、`run_id`、`run_sha256` 和 task outcomes；运行态声明记录 fresh structured observation，source-only 写 `unverified_boundaries`，high-risk 写不同 Agent 的 `independent_review`，并用 `artifact_sha256` 绑定 task/run 目录的 canonical `independent-review.md` 及其 reviewer/time/PASS 元数据。local review/hash 只做证据一致性校验；baseline `profile: high-assurance` 还需记录在独立可信边界验证过的 `external_provenance`。

如果 `package.json`、README、CI 配置和实际可运行命令冲突，以当前实际可运行命令为准，并在本文档里写清楚原因。
