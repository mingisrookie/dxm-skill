# 领域术语格式

优先使用项目现有术语表和上下文地图。没有合适文档时，只有在获准写入且已有确认内容后才创建新文件；只读讨论不自动创建 `CONTEXT.md`。

一个词条记录业务名称、一两句定义、所属上下文，以及必要时容易混淆的词或不同含义。解释它是什么、边界在哪里，不复制实现代码。

不同领域中相同词语可以有不同含义。已有上下文地图时按它确定归属；不确定且会改变业务关系时再提问，不从文件名硬猜。

地图只需说明各领域职责、知识位置和主要关系，不要求每个项目采用相同目录。未确认定义明确标为待确认，不能写成既定业务事实。
