# 决策记录格式

先沿用项目已有记录位置和格式。仅在获准写入且确有需要时创建文档或目录；没有写入授权时在对话中给出草稿。

最小记录可以只有标题与一段正文：问题是什么、决定如何处理、为什么、主要代价是什么。只有能帮助未来理解时才加状态、备选方案和后果。

只有难以逆转、缺少背景会令人困惑、并且存在真实取舍的决定才值得单独记录。容易改变的普通选择不用建立 ADR。

项目使用数字编号时，写入前检查已用编号和并发修改；不要覆盖已有文件。需要记录尚未批准的决定时标为提议，明确接受或否决仍待谁确认。
