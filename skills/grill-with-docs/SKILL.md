---
name: grill-with-docs
description: Use when stress-testing a plan against existing code, docs, domain terminology, glossary, ADRs, or documented decisions.
disable-model-invocation: true
---

Run a `grilling` session while using `domain-modeling`.

This skill is only the router: `grilling` owns the interview cadence, and `domain-modeling` owns `CONTEXT.md`, `CONTEXT-MAP.md`, and ADR handling.
